package hu.guidance.filmregister.repository;

import hu.guidance.filmregister.model.CodecFormat;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CodecFormatRepository extends JpaRepository<CodecFormat, Long> {
}
